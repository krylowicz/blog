query getAllPosts($limit: Int!, $cursor: String) {
  getAllPosts(limit: $limit, cursor: $cursor) {
    hasMore
    posts {
      id
      createdAt
      updatedAt
      title
      points
      textSnippet
      voteStatus
      author {
        id
        username
      }
    }
  }
}
